#pragma once

namespace Type {
	
	enum Mob {
		one
	};

	enum Content {
		Road,
		Grass,
		Bush,
		Rock
	};

	enum Weapon {
		two
	};

	enum Direct {
		Left,
		Top,
		Right,
		Down
	};

	enum Options {
		Start,
		Settings,
		Credits,
		Exit,
		FPS,
		Volume,
		Undo,
		Miss
	};
}